# Public, non-secret API origin. Leave unset for local Vite (it proxies `/api`
# to the inquiry API on 127.0.0.1:8787) and for a same-domain production deploy.
#
# Set this only when the site and the API are hosted on different origins.
# VITE_API_BASE_URL=https://api.example.com
