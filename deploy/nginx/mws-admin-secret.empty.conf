# Optional. The sample vhost proxies unknown paths to Node (@node), so the
# secret admin prefix does not need to live in Nginx.
# `npm run render:admin-nginx` can still emit an explicit location if you want
# one. Do not commit a generated file that contains ADMIN_SECRET_PATH.
